@model List<SelectListItem>
           <div class="vocation">
<div class="uew-select">
    <div class="uew-select-value ue-state-default" style="width: 118px;">
  
    <em class="uew-select-text">@(Model.Count() > 0 ? Model.Where(s => s.Selected == true).FirstOrDefault() != null ? Model.Where(s => s.Selected == true).FirstOrDefault().Text : Model.FirstOrDefault().Text : "")</em><em class="uew-icon uew-icon-triangle-1-s"></em></div>
  
   @Html.DropDownList("sorter", Model, new { @class = "select1" })
    </div>
    </div>
 
@*<div>
    排序:@Html.DropDownList("sorter", Model, new { @class = "sorter" })</div>*@
